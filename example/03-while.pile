\ First way of doing it 
10 while dup 0 >= do \ From 10 to 0
  dup dump
  1 -
end drop

\ Another way
10 0 while dup2 >= do \ From 0 to 10 using `dup2`
  dup dump
  1 +
end drop drop

\ Only print even numbers
10 0 while dup2 >= do
   dup 2 mod 0 = if
      dup dump
   end
   1 +
end drop drop
